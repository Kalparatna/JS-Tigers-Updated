{
  "name": "vendor-management-app",
  "version": "1.0.0",
  "description": "Vendor Management System with Google Auth",
  "main": "api/server.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "nodemon api/server.js",
    "client": "cd ../client && npm run dev",
    "build": "cd ../client && npm run build",
    "start": "node api/server.js"
  },
  "dependencies": {
    "axios": "^1.11.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "google-auth-library": "^9.0.0",
    "mongoose": "^7.5.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "nodemon": "^3.0.1"
  }
}
